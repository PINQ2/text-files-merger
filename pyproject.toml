[project]
name = "text-files-merger"
version = "0.1.0"
description = "Merge two text files into one — AHP platform base kit"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["text_merger"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
